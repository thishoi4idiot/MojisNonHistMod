add_namespace = chi_restoration

country_event = {
	id = chi_restoration.1
	title = chi_restoration.1.t
	desc = chi_restoration.1.d

	picture = GFX_report_event_generic_china

	fire_only_once = yes

	is_triggered_only = yes

	option = {
		name = chi_restoration.1.a

		set_politics = {
			ruling_party = democratic
			elections_allowed = yes
			last_election = "1936.1.1"
			election_frequency = 48
		}
		hold_election = CHI

		set_cosmetic_tag = CHI

		add_popularity = {
			ideology = democratic
			popularity = 0.35
		}
		add_popularity = {
			ideology = neutrality
			popularity = -0.15
		}
		add_popularity = {
			ideology = fascism
			popularity = -0.15
		}

		add_stability = 0.15
		add_war_support = -0.15

		IF = {
			limit = {
				has_country_leader_with_trait = CHI_blueshirt_spymaster
			}
			remove_country_leader_trait = CHI_blueshirt_spymaster
		}
	}
}
